{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run AI Agent",
      "type": "shell",
      "command": "docker compose up --build",
      "options": {
        "cwd": "${workspaceFolder:ai-agent}"
      }
    },
    {
      "label": "Run Scout",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder:scout}"
      }
    }
  ]
}